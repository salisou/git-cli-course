# git fetch

Scarica commit e riferimenti dal remoto senza integrare automaticamente le modifiche nel working tree.

```bash
git fetch origin
git fetch --all
git log --oneline --all --decorate
```

**Nota:** fetch aggiorna le informazioni locali sul remoto; pull recupera e poi integra.
