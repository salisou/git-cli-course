# git remote

## Cos'è
Gestisce i riferimenti ai repository remoti.

## Sintassi
~~~bash
git remote -v
git remote add origin <url>
git remote get-url origin
git remote set-url origin <url>
~~~

## Esercizio
Aggiungi `origin` e verifica gli URL con `git remote -v`.