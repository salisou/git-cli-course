# git show

## Cos'è
Mostra dettagli e diff di un oggetto, normalmente un commit.

```bash
git show HEAD
git show <commit-sha>
```

## Esercizio
Prendi lo SHA di un commit con `git log --oneline` e analizzalo con `git show`.
