# git blame

Mostra, riga per riga, il commit e l'autore associati al contenuto corrente.

```bash
git blame README.md
```

È uno strumento di analisi, non di colpevolizzazione. Usalo per capire quando e perché una riga è stata introdotta, poi approfondisci con `git show`.
