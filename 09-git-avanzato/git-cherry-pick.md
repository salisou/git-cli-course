# git cherry-pick

Applica su un branch le modifiche introdotte da uno o più commit esistenti.

```bash
git switch main
git cherry-pick <sha>
```

Utile per portare una correzione mirata senza integrare tutta una feature branch.
