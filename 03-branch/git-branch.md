# git branch

## Esempi
```bash
git branch
git branch -a
git branch feature/login
git branch -d feature/login
git branch -m old-name new-name
```

Per creare e passare direttamente a un branch moderno è preferibile `git switch -c`.
