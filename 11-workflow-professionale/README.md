# 11 — Workflow professionale

```
main
  └── feature/nome-feature
        ├── commit piccolo
        ├── test
        └── pull request
                 ↓
             code review
                 ↓
              merge
```

Commit chiari: feat, fix, docs, refactor, test, chore.

Checklist PR:
```bash
git status
git diff
git diff --staged
git log -5 --oneline
```
