# git reflog

Registra i movimenti locali di HEAD e di altri riferimenti.

```bash
git reflog
git switch -c recovery <sha>
```

Può permettere di individuare un commit dopo un reset accidentale, finché il riferimento è disponibile localmente.
