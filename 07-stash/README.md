# 07 — git stash

Metti temporaneamente da parte modifiche locali non ancora committate.

```bash
git stash
git stash push -m "lavoro temporaneo"
git stash list
git stash pop
git stash apply
git stash drop
```

Laboratorio: modifica un file, crea uno stash, cambia branch e recupera le modifiche.
